# =============================================================================
# CapMatch Data Room — Environment template
# =============================================================================
# Copy this file to .env.local and fill in the values.
# For local development with Docker (Supabase CLI): use the "Local (Docker)" block.
# For production / hosted Supabase: use your project's URL and keys from the dashboard.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (required)
# -----------------------------------------------------------------------------
# Project URL — from Supabase dashboard or local: http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=
# Anon (public) key — safe to use in browser; RLS enforces security
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: service role key — server-only; bypasses RLS. Use for admin tasks.
# Do not expose in client-side code.
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# Local development (Docker)
# -----------------------------------------------------------------------------
# After running `supabase start`, use these in .env.local:
#
#   NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
#   NEXT_PUBLIC_SUPABASE_ANON_KEY=<from `supabase status` output>
#   SUPABASE_SERVICE_ROLE_KEY=<from `supabase status` output>
#
# Run: npx supabase status  (from project root with Supabase CLI installed)
# =============================================================================
