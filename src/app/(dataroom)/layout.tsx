import DataRoomLayout from '@/components/DataRoomLayout';

export default function DataroomLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <DataRoomLayout>{children}</DataRoomLayout>;
}
